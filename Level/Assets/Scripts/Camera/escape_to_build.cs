using UnityEngine;
using System.Collections;

public class escape_to_build : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if (Input.GetKeyDown (KeyCode.Space) == true) {
			
		}
	
	}
}
